Attribute VB_Name = "MODULE11"
Global nam(1000) As String * 25
Global value(1000), n%, temp1$, temp2$, temp3$, numclass%
Global yy%, z%, j%, minvalue, maxvalue, dataname$, kind%
Global lowclass(), highclass(), m1(), a1()
Global cl(1000) As Integer, sumx, sumxx, mean, sd
Global nclass() As Integer
Global classkind$, check%, adder

