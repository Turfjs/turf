var t = require('../index'),
  should = require('should')

describe('square', function(){
  it('should return a bbox representing the smallest square that contains the input bbox', function(done){
    
    done()
  })
}) 