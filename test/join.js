var g = require('../index')

describe('join', function(){
  describe('#index', function(){
    xit('should add the properties of the wrapping polygon to the point', function(done){

      done()
    })
  })
}) 